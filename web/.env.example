# OpenRouter API 키 (없으면 /api/parse 는 503 + 규칙 미리보기만 동작)
OPENROUTER_API_KEY=
# 기본 방어자 모델
DEFENDER_MODEL=qwen/qwen3-8b
# 프로세스 전체 일일 LLM 호출 상한
DAILY_CALL_CAP=500
PORT=3000
# smoke 데이터 디렉터리 (없으면 ./data 사용)
SMOKE_DIR=../smoke
